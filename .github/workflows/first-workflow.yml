name: First Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "Hello kondaiah"
            echo "what's upp"
      - name: Print Message
        run: echo "learn smartly"
